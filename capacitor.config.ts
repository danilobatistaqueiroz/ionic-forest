import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.labs.modalanimations',
  appName: 'modal-animations',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
